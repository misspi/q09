<%= title t(:title) %>

<table>
  <thead><tr>
    <td><%= t(:username) %>
  </tr></thead>
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to h(user.username), user_path(user) %></td>
    </tr>
    <% end %>
</table>